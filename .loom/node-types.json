{
  "types": {
    "view": {
      "label": "SwiftUI View",
      "description": "User-facing screen or component (SwiftUI).",
      "color": "#60a5fa",
      "icon": "monitor",
      "fields": [
        { "name": "screen", "type": "string", "required": false }
      ]
    },
    "viewmodel": {
      "label": "ViewModel",
      "description": "State management + business logic (@Observable).",
      "color": "#34d399",
      "icon": "cpu",
      "fields": [
        { "name": "state", "type": "string", "required": false }
      ]
    },
    "repository": {
      "label": "Repository",
      "description": "Data access abstraction (local + remote).",
      "color": "#fbbf24",
      "icon": "database",
      "fields": [
        { "name": "entities", "type": "string", "required": false }
      ]
    },
    "database": {
      "label": "Local Database",
      "description": "On-device persistence (SQLite via GRDB).",
      "color": "#a78bfa",
      "icon": "database",
      "fields": [
        { "name": "engine", "type": "enum", "values": ["sqlite", "swiftdata", "coredata", "file", "memory"], "required": false },
        { "name": "tables", "type": "string", "required": false }
      ]
    },
    "sync-engine": {
      "label": "Sync Engine",
      "description": "Background fetch / cron updater with retry + backoff.",
      "color": "#f97316",
      "icon": "refresh-cw",
      "fields": [
        { "name": "schedule", "type": "string", "required": false },
        { "name": "retry_policy", "type": "string", "required": false }
      ]
    },
    "api-client": {
      "label": "API Client",
      "description": "Remote HTTP client (URLSession + async/await).",
      "color": "#94a3b8",
      "icon": "globe",
      "fields": [
        { "name": "base_url", "type": "string", "required": false }
      ]
    },
    "model": {
      "label": "Domain Model",
      "description": "Core data entity (Sendable struct).",
      "color": "#ec4899",
      "icon": "box",
      "fields": [
        { "name": "properties", "type": "string", "required": false }
      ]
    }
  }
}

